<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Success</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Poppins Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #f4f4f4;
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      background-color: #fff;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
      text-align: center;
      width: 90%;
      max-width: 600px;
    }
    h1 {
      color: #4CAF50;
      margin-bottom: 10px;
    }
    h2 {
      color: #ba5a49;
      margin-bottom: 10px;
    }
    p {
      font-size: 18px;
      margin-bottom: 20px;
      color: #555;
    }
    .order-details {
      text-align: left;
      margin: 20px 0;
    }
    .order-details p {
      margin: 5px 0;
    }
    .order-details ul {
      list-style-type: none; /* Remove default list style */
      padding: 0; /* Remove default padding */
    }
    .order-details li {
      padding: 5px 0;
      border-bottom: 1px solid #eee; /* Add a subtle separator */
    }
    .order-details li:last-child {
      border-bottom: none; /* Remove the border for the last item */
    }
    .buttons {
      display: flex;
      gap: 10px;
      justify-content: center;
    }
    .buttons a {
      text-decoration: none;
      color: white;
      background-color: #4CAF50;
      padding: 10px 20px;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }
    .buttons a:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <div class="container">
    <% if(order.orderStatus !== 'Failed'){ %>
      <h1>Order Placed Successfully!</h1>
      <p>Thank you for your purchase, <%= order.userId.firstname %>!</p>
    <% }else{ %>
      <h2>Payment Failed. Please Try Again</h2>
    <% } %>

    <div class="order-details">
      <h3>Order Details</h3>
      <p><strong>Order ID:</strong> <%= order._id %></p>
      <p><strong>Items:</strong></p>
      <ul>
        <% order.items.forEach(product => { %>
          <li><%= product.productId.name %> X <%= product.quantity %></li>
        <% }); %>
      </ul>
      <p><strong>Total Amount:</strong> ₹<%= order.totalAmount %></p>
    </div>

    <div class="buttons">
      <a href="/user/shop" class="btn">Continue Shopping</a>
      <a href="/user/profile/orders" class="btn">View My Orders</a>
    </div>
  </div>

  <!-- Bootstrap JS and dependencies (optional, for further interactivity) -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
